-- =============================================================
-- 膳衡平台管理端菜单精简
-- 停用与业务无关的若依自带模块（菜单+按钮），使其不在左侧导航显示
-- 保留：系统管理(1)、租户管理(6)、膳衡业务(2000)
-- 停用：
--   menu_id 2 系统监控（在线用户/缓存监控/Admin监控/snailjob）
--   menu_id 3 系统工具（代码生成）
--   menu_id 5 系统demo（演示页面）
--   menu_id 4 PLUS官网（外链跳转）
-- 回滚方式：把 status 改回 '0' 即可重新显示
-- =============================================================
USE `ry-vue`;

UPDATE `sys_menu`
SET `status` = '1'
WHERE `menu_id` IN (4, 2, 3, 5)
   OR `parent_id` IN (2, 3, 5);