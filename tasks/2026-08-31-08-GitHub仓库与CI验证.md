# 任务日志 2026-08-31-08 · GitHub 仓库打通 + CI 构建管线验证

## 一、目标

把代码同步切换到正式链路：本地 `git push` → GitHub Actions 构建/推镜像（不再手动 scp 到服务器）。

## 二、已完成

### 1. 代码推送
- 用户已配置 Git Credential Manager，改用 HTTPS：`git remote add origin https://github.com/Jerry-Guo1020/healthyapp.git` + `git push -u origin main` **成功**。
- 此前 SSH 方案（`ssh-ed25519` 公钥）生成后未采用，`~/.ssh/config` 中的 `Host github.com` 段为死配置，可忽略。

### 2. CI 工作流修正（4 处）
| 问题 | 修复 |
| --- | --- |
| `./mvnw` 不存在（项目无 wrapper） | 改用系统 `mvn` |
| GHCR 登录用 PAT（`REGISTRY_PASSWORD`） | 改 `docker/login-action` + `GITHUB_TOKEN`（`permissions: packages: write`） |
| GHCR 镜像名含大写（`Jerry-Guo1020`）会被拒 | 硬编码小写 `jerry-guo1020/healthyapp/api\|web` |
| `if` 条件里直接用 secrets 不合法 | 改为 step 内检查 `$GITHUB_OUTPUT` 再决定是否部署 |

### 3. 前端镜像构建修复
- 根因：`maneger-web` 无 lockfile 且 `package.json` 无 `packageManager`，Dockerfile 的 `corepack enable` 无法激活 pnpm 版本。
- 修复：Dockerfile 改 `npm install -g pnpm@9.14.2` 固定版本；新增 `.dockerignore` 缩小构建上下文（排除 .git/api/doc/tasks/node_modules）。

### 4. 验证结果
- `api-ci` ✅：Maven 打包 → Docker 构建 → 推 GHCR `ghcr.io/jerry-guo1020/healthyapp/api`。
- `manager-ui-ci` ✅：pnpm 构建 → 推 GHCR `ghcr.io/jerry-guo1020/healthyapp/web`。
- 两个镜像包已存在于 GHCR（默认 private，401）。

## 三、遗留（部署阶段待办）

1. **GitHub secrets**：`SERVER_HOST` / `SERVER_USER` / `SERVER_SSH_KEY`（供 deploy job SSH 部署）。
2. **GHCR 拉取权限**：私有包，服务器 `docker pull` 需登录 GHCR（PAT read:packages 或改包可见性）。
3. **服务器侧**：`/opt/shanheng` 放 `docker-compose.yml` + `.env`；导入 `ry_vue_5.X.sql` + `ry_job.sql` + `shanheng_business.sql`。
4. **内存**：测试服务器仅 2GB 且已跑 3 个 MySQL 等，正式起 MySQL8+Redis+RuoYi 需调优或腾资源。

## 四、下一步

1. 生成服务器专用 deploy key（公钥入 authorized_keys，私钥交用户填 secrets）。
2. 服务器准备 compose/.env + 导 SQL。
3. 拉镜像起服务，验证 `/prod-api`（管理端）与 `/app/v1`（App）接口。